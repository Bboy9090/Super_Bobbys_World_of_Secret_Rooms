{
  "version": "1.0.0",
  "tools": [
    {
      "id": "android.adb",
      "name": "Android Debug Bridge",
      "binary": "adb",
      "sha256": "REPLACE_WITH_SHA256",
      "platforms": ["win32", "darwin", "linux"],
      "allowed_subcommands": ["devices", "version", "start-server", "kill-server", "shell", "bugreport", "logcat", "pull", "push"],
      "notes": "Diagnostics and authorized operations only. No circumvention workflows allowed.",
      "risk": "medium"
    },
    {
      "id": "android.fastboot",
      "name": "Fastboot",
      "binary": "fastboot",
      "sha256": "REPLACE_WITH_SHA256",
      "platforms": ["win32", "darwin", "linux"],
      "allowed_subcommands": ["devices", "getvar", "oem", "reboot", "reboot-recovery", "reboot-bootloader"],
      "notes": "Read-only operations and authorized flashing only. Bootloader must be unlocked for flash operations.",
      "risk": "high",
      "restricted_subcommands": {
        "flash": {
          "requires_gates": ["GATE_OWNERSHIP_ATTESTATION", "GATE_DEVICE_AUTHORIZATION", "GATE_DESTRUCTIVE_CONFIRMATION"]
        },
        "erase": {
          "requires_gates": ["GATE_OWNERSHIP_ATTESTATION", "GATE_DEVICE_AUTHORIZATION", "GATE_DESTRUCTIVE_CONFIRMATION"]
        }
      }
    },
    {
      "id": "ios.ideviceinfo",
      "name": "libimobiledevice - ideviceinfo",
      "binary": "ideviceinfo",
      "sha256": "REPLACE_WITH_SHA256",
      "platforms": ["darwin", "linux"],
      "allowed_subcommands": [],
      "allowed_args": ["-s", "-k", "ProductType", "-k", "ProductVersion", "-k", "UniqueDeviceID"],
      "notes": "Read-only device metadata when available.",
      "risk": "low"
    },
    {
      "id": "ios.irecovery",
      "name": "libimobiledevice - irecovery",
      "binary": "irecovery",
      "sha256": "REPLACE_WITH_SHA256",
      "platforms": ["darwin", "linux"],
      "allowed_args": ["-q"],
      "notes": "Read-only recovery mode detection.",
      "risk": "low"
    },
    {
      "id": "system.usb.enumerate",
      "name": "USB Device Enumeration",
      "binary": "system",
      "sha256": "N/A",
      "platforms": ["win32", "darwin", "linux"],
      "notes": "Read-only USB device enumeration for detection.",
      "risk": "low"
    }
  ]
}
